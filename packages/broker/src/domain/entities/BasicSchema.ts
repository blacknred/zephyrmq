export enum BasicSchema {
  SegmentPointer = "segment_pointer_schema",
  MessageMetadata = "message_metadata_schema",
}

// const schemas: Record<BasicSchema, ISchema<unknown>> = {
//   [BasicSchema.MessageMetadataSchema]: messageMetadataSchema,
//   [BasicSchema.SegmentPointerSchema]: segmentPointerSchema,
// };

// export default schemas;
